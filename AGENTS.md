## Cursor Cloud specific instructions

This is a freshly initialized repository with no application code yet. The repo currently contains only a `README.md` placeholder.

- **No dependencies** to install (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.)
- **No services** to run
- **No build system** configured
- **No tests** to execute
- **No linting** configured

When application code is added, this section should be updated with relevant development setup instructions, service startup caveats, and testing guidance.
